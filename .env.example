# Example environment variables for Riva
# Copy to .env if you want (but do NOT commit your real .env with secrets).
# Note: This project does not auto-load .env; set these in Windows env vars or your .bat file.

# Force Whisper language (auto-detect if unset). Examples: en, bn
RIVA_STT_LANG=

# How long (seconds) Riva stays awake after you say "hi riva" / "hey riva".
# Default is effectively "until exit/sleep".
RIVA_AWAKE_WINDOW_SEC=

# Optional: choose a preferred Windows voice name (e.g. "Microsoft Zira Desktop")
RIVA_VOICE=
